// Flip this to false once your Supabase project is set up and
// services/supabase/supabaseClient.js has real credentials in it.
// Nothing else in the app needs to change — hooks and screens stay identical.
export const USE_MOCK_BACKEND = true;